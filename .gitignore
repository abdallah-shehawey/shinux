build/

# The published tree. Assembled on every publish from the packages in the
# "pool" release and uploaded to Pages as an artifact -- committing it is what
# had grown this repository to 4.5 GB, for files nobody clones it to read.
docs/
.gnupg/
private-key.asc
*.log

.tools/
.fuse_hidden*

# Electron is 194 MB in one file, past what GitHub will accept in a push.
# Generated by packages/whatsapp-desktop/refresh.sh before a build.
packages/whatsapp-desktop/src/

# Staged by the publish workflow from the whatsapp-desktop repository.
.wa-src/
