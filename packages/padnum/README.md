# padnum

Zero-pad the numeric prefix of filenames so they sort correctly, with undo.

    padnum -n          # see what would change
    padnum -p 3        # force three digits
    padnum --undo      # put it back

See `man 1 padnum`.
