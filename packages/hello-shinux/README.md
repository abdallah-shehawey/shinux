# hello-shinux

Placeholder package used to prove the repository plumbing end to end.
Replace `src/usr/bin/hello-shinux` with your real program, bump `VERSION`
in `packages/hello-shinux/metadata.env`, and rebuild.
