# update-every-thing

Run every system update in one pass, with a summary at the end.
Detects dnf/yum or apt and uses the right commands for each.

    update-every-thing
    update-every-thing -c 3 --with-nvidia

See `man 1 update-every-thing`.
