# shinux-scripts

A metapackage that installs every command from the Shinux repository.

    sudo dnf install shinux-scripts
    sudo apt install shinux-scripts

It ships no files of its own. Install the individual packages instead if you
only want some of them.
